/**
 * GSPNS / NSmart Public Transport API (Novi Sad)
 * Unofficial documentation of the reverse-engineered API behind the GSPNS (JGSP \"Novi Sad\") public transport network — the same API used by the **NSmart** mobile app (`buslogic.nsmartapp`).  The API is served by **BusLogic/NSmart** (`online.nsmart.rs`) and provides:  * the static transport network (cities → stations → line pairs), * real-time vehicle positions and arrival predictions per station, * electronic ticketing / wallet / card management endpoints (login, e-wallet, top-up…).  ## Reverse engineering provenance  * Endpoint paths, class names and the shared API key were recovered from the   NSmart 4.6 Android APK (see [FmasterofU/NSmart-RE](https://github.com/FmasterofU/NSmart-RE)). * Response schemas below were verified against the live API in September 2026. * The API key (`4670f468049bbee2260`) is compiled into the public APK as   `company_api_key` and is the same for every install — treat it as public knowledge.  ## Authentication  Every request must send the header:  ``` X-Api-Authentication: 4670f468049bbee2260 ```  All endpoints in this document include that header in their `security` requirements.  ## Notes & quirks (learned the hard way)  * The giant network payload (`networkextended.php`) is **not valid JSON when served   over a slow connection** — the server truncates / corrupts the stream mid-body   (raw control characters, null-byte runs, dropped chunks). The NSmart app   apparently tolerates this; re-request until you get a parseable body. * `networkextended.php` ignores semantically different `action` values   (`get_network`, `get_trips`, `get_schedule` return the same cities payload as   `get_cities_extended`). * `announcement.php` returns `[{\"success\":false,\"code\":3}]` for an unknown   `station_uid`. For a *known* station with nothing due it returns a single   placeholder entry (`{\"just_coordinates\":\"1\", ...}`) instead of an empty array. * A vehicle without a GPS fix is reported at `lat: \"0.00000000\"`, `lng: \"0.00000000\"`   (often with an empty `garageNo`) — treat `0,0` as \"position unknown\". * `networkextended.php` also answers a plain `GET` with no parameters, and the payload   holds far more than `cities`: the flat `stations` and `lines` lists are what most   clients want (see the `Network` schema and `docs/data-notes.md`). * Timetables (scheduled departures) are **not** served by this API — see   `docs/timetables.md` for the official gspns.co.rs pages that carry them. * `ibfm=TM00000` means \"all lines\"; pass a line code (e.g. `7A`) to filter. * Serbian text is `UTF-8`; strings may come back with JS-style unicode escapes. 
 *
 * The version of the OpenAPI document: 1.2.0
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.26.0-SNAPSHOT.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * Station.h
 *
 * A station (stop) from the network payload.
 */

#ifndef ORG_OPENAPITOOLS_CLIENT_MODEL_Station_H_
#define ORG_OPENAPITOOLS_CLIENT_MODEL_Station_H_

#include <boost/optional.hpp>

#include "jgsp/ModelBase.h"

#include "jgsp/model/Station_coordinates.h"
#include "jgsp/model/Station_lines_for_station_additional_data_inner.h"
#include <cpprest/details/basic_types.h>
#include <vector>

namespace org {
namespace openapitools {
namespace client {
namespace model {

class Station_coordinates;
class Station_lines_for_station_additional_data_inner;


/// <summary>
/// A station (stop) from the network payload.
/// </summary>
class  Station
    : public ModelBase
{
public:
    Station();
    virtual ~Station();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// Station members


    /// <summary>
    /// Station ID — the value used as &#x60;station_uid&#x60; and in &#x60;Line.all_stations&#x60;.
    /// </summary>
    int32_t getId() const;
    bool idIsSet() const;
    void unsetId();
    void setId(int32_t value);

    utility::string_t getName() const;
    bool nameIsSet() const;
    void unsetName();
    void setName(const utility::string_t& value);

    /// <summary>
    /// Operator&#39;s own station code (not the numeric &#x60;id&#x60;).
    /// </summary>
    utility::string_t getStationId() const;
    bool stationIdIsSet() const;
    void unsetStation_id();
    void setStationId(const utility::string_t& value);

    utility::string_t getSlugs() const;
    bool slugsIsSet() const;
    void unsetSlugs();
    void setSlugs(const utility::string_t& value);

    int32_t getCityId() const;
    bool cityIdIsSet() const;
    void unsetCity_id();
    void setCityId(int32_t value);

    utility::string_t getCityName() const;
    bool cityNameIsSet() const;
    void unsetCity_name();
    void setCityName(const utility::string_t& value);

    std::shared_ptr<Station_coordinates> getCoordinates() const;
    bool coordinatesIsSet() const;
    void unsetCoordinates();
    void setCoordinates(const std::shared_ptr<Station_coordinates>& value);

    /// <summary>
    /// Line-pair IDs serving the station (may contain repeats).
    /// </summary>
    std::vector<int32_t> getPairs() const;
    bool pairsIsSet() const;
    void unsetPairs();
    void setPairs(std::vector<int32_t> value);

    int32_t getImportanceOrder() const;
    bool importanceOrderIsSet() const;
    void unsetImportance_order();
    void setImportanceOrder(int32_t value);

    bool isTrainStation() const;
    bool trainStationIsSet() const;
    void unsetTrain_station();
    void setTrainStation(bool value);

    /// <summary>
    /// Display numbers of the lines that stop here.
    /// </summary>
    std::vector<utility::string_t> getLinesForStation() const;
    bool linesForStationIsSet() const;
    void unsetLines_for_station();
    void setLinesForStation(const std::vector<utility::string_t>& value);

    std::vector<std::shared_ptr<Station_lines_for_station_additional_data_inner>> getLinesForStationAdditionalData() const;
    bool linesForStationAdditionalDataIsSet() const;
    void unsetLines_for_station_additional_data();
    void setLinesForStationAdditionalData(const std::vector<std::shared_ptr<Station_lines_for_station_additional_data_inner>>& value);


protected:
    int32_t m_Id;
    bool m_IdIsSet;

    utility::string_t m_Name;
    bool m_NameIsSet;

    utility::string_t m_Station_id;
    bool m_Station_idIsSet;

    utility::string_t m_Slugs;
    bool m_SlugsIsSet;

    int32_t m_City_id;
    bool m_City_idIsSet;

    utility::string_t m_City_name;
    bool m_City_nameIsSet;

    std::shared_ptr<Station_coordinates> m_Coordinates;
    bool m_CoordinatesIsSet;

    std::vector<int32_t> m_Pairs;
    bool m_PairsIsSet;

    int32_t m_Importance_order;
    bool m_Importance_orderIsSet;

    bool m_Train_station;
    bool m_Train_stationIsSet;

    std::vector<utility::string_t> m_Lines_for_station;
    bool m_Lines_for_stationIsSet;

    std::vector<std::shared_ptr<Station_lines_for_station_additional_data_inner>> m_Lines_for_station_additional_data;
    bool m_Lines_for_station_additional_dataIsSet;

};


}
}
}
}

#endif /* ORG_OPENAPITOOLS_CLIENT_MODEL_Station_H_ */
