# jgsp-client
Unofficial documentation of the reverse-engineered API behind the GSPNS (JGSP \"Novi Sad\")
public transport network — the same API used by the **NSmart** mobile app
(`buslogic.nsmartapp`).

The API is served by **BusLogic/NSmart** (`online.nsmart.rs`) and provides:

* the static transport network (cities → stations → line pairs),
* real-time vehicle positions and arrival predictions per station,
* electronic ticketing / wallet / card management endpoints (login, e-wallet, top-up…).

## Reverse engineering provenance

* Endpoint paths, class names and the shared API key were recovered from the
  NSmart 4.6 Android APK (see [FmasterofU/NSmart-RE](https://github.com/FmasterofU/NSmart-RE)).
* Response schemas below were verified against the live API in September 2026.
* The API key (`4670f468049bbee2260`) is compiled into the public APK as
  `company_api_key` and is the same for every install — treat it as public knowledge.

## Authentication

Every request must send the header:

```
X-Api-Authentication: 4670f468049bbee2260
```

All endpoints in this document include that header in their `security` requirements.

## Notes & quirks (learned the hard way)

* The giant network payload (`networkextended.php`) is **not valid JSON when served
  over a slow connection** — the server truncates / corrupts the stream mid-body
  (raw control characters, null-byte runs, dropped chunks). The NSmart app
  apparently tolerates this; re-request until you get a parseable body.
* `networkextended.php` ignores semantically different `action` values
  (`get_network`, `get_trips`, `get_schedule` return the same cities payload as
  `get_cities_extended`).
* `announcement.php` returns `[{\"success\":false,\"code\":3}]` for an unknown
  `station_uid`. For a *known* station with nothing due it returns a single
  placeholder entry (`{\"just_coordinates\":\"1\", ...}`) instead of an empty array.
* A vehicle without a GPS fix is reported at `lat: \"0.00000000\"`, `lng: \"0.00000000\"`
  (often with an empty `garageNo`) — treat `0,0` as \"position unknown\".
* `networkextended.php` also answers a plain `GET` with no parameters, and the payload
  holds far more than `cities`: the flat `stations` and `lines` lists are what most
  clients want (see the `Network` schema and `docs/data-notes.md`).
* Timetables (scheduled departures) are **not** served by this API — see
  `docs/timetables.md` for the official gspns.co.rs pages that carry them.
* `ibfm=TM00000` means \"all lines\"; pass a line code (e.g. `7A`) to filter.
* Serbian text is `UTF-8`; strings may come back with JS-style unicode escapes.


This Python package is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.2.0
- Package version: 1.0.0
- Generator version: 7.26.0-SNAPSHOT
- Build package: org.openapitools.codegen.languages.PythonClientCodegen

## Requirements.

Python 3.10+

## Installation & Usage
### pip install

If the python package is hosted on a repository, you can install directly using:

```sh
pip install git+https://github.com/GIT_USER_ID/GIT_REPO_ID.git
```
(you may need to run `pip` with root permission: `sudo pip install git+https://github.com/GIT_USER_ID/GIT_REPO_ID.git`)

Then import the package:
```python
import jgsp_client
```

### Setuptools

Install via [Setuptools](http://pypi.python.org/pypi/setuptools).

```sh
python setup.py install --user
```
(or `sudo python setup.py install` to install the package for all users)

Then import the package:
```python
import jgsp_client
```

### Tests

Execute `pytest` to run the tests.

## Getting Started

Please follow the [installation procedure](#installation--usage) and then run the following:

```python

import jgsp_client
from jgsp_client.rest import ApiException
from pprint import pprint

# Defining the host is optional and defaults to https://online.nsmart.rs
# See configuration.py for a list of all supported configuration parameters.
configuration = jgsp_client.Configuration(
    host = "https://online.nsmart.rs"
)

# The client must configure the authentication and authorization parameters
# in accordance with the API server security policy.
# Examples for each auth method are provided below, use the example that
# satisfies your auth use case.

# Configure API key authorization: ApiKeyAuth
configuration.api_key['ApiKeyAuth'] = os.environ["API_KEY"]

# Uncomment below to setup prefix (e.g. Bearer) for API key, if needed
# configuration.api_key_prefix['ApiKeyAuth'] = 'Bearer'


# Enter a context with an instance of the API client
with jgsp_client.ApiClient(configuration) as api_client:
    # Create an instance of the API class
    api_instance = jgsp_client.AccountApi(api_client)
    action = 'action_example' # str | Account action verb.
    email = 'email_example' # str | Used by login/register/reset flows. (optional)
    password = 'password_example' # str |  (optional)
    first_name = 'first_name_example' # str |  (optional)
    last_name = 'last_name_example' # str |  (optional)

    try:
        # Account actions (login, register, reset password…)
        api_response = api_instance.account_action(action, email=email, password=password, first_name=first_name, last_name=last_name)
        print("The response of AccountApi->account_action:\n")
        pprint(api_response)
    except ApiException as e:
        print("Exception when calling AccountApi->account_action: %s\n" % e)

```

## Documentation for API Endpoints

All URIs are relative to *https://online.nsmart.rs*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*AccountApi* | [**account_action**](docs/AccountApi.md#account_action) | **POST** /publicapi/v1/rest_options/android_login.php | Account actions (login, register, reset password…)
*MiscApi* | [**legacy_api**](docs/MiscApi.md#legacy_api) | **POST** /api/api.php | Generic internal API dispatcher
*NetworkApi* | [**get_network**](docs/NetworkApi.md#get_network) | **POST** /publicapi/v1/networkextended.php | Get the full transport network (cities with stations)
*NetworkApi* | [**get_network_get**](docs/NetworkApi.md#get_network_get) | **GET** /publicapi/v1/networkextended.php | Get the transport network (GET variant)
*PaymentsApi* | [**allsecure_payment**](docs/PaymentsApi.md#allsecure_payment) | **POST** /publicapi/v1/rest_options/android_allsecure.php | AllSecure payment operations
*RealtimeApi* | [**get_station_announcements**](docs/RealtimeApi.md#get_station_announcements) | **GET** /publicapi/v1/announcement/announcement.php | Get live arrivals &amp; vehicle positions for a station
*WalletCardsApi* | [**add_or_connect_card**](docs/WalletCardsApi.md#add_or_connect_card) | **POST** /publicapi/v1/rest_options/android_add_or_connect_card.php | Add or connect a transport card
*WalletCardsApi* | [**additional_options**](docs/WalletCardsApi.md#additional_options) | **POST** /publicapi/v1/rest_options/android_additional_options.php | Additional card/ticket options
*WalletCardsApi* | [**additional_settings**](docs/WalletCardsApi.md#additional_settings) | **POST** /publicapi/v1/rest_options/android_additional_settings.php | Additional settings
*WalletCardsApi* | [**prepaid_cards_log**](docs/WalletCardsApi.md#prepaid_cards_log) | **POST** /publicapi/v1/rest_options/android_prepaid_cards_log_online.php | Prepaid card online log


## Documentation For Models

 - [AccountAction200Response](docs/AccountAction200Response.md)
 - [Announcement](docs/Announcement.md)
 - [AnnouncementAllStationsInner](docs/AnnouncementAllStationsInner.md)
 - [ApiStatus](docs/ApiStatus.md)
 - [City](docs/City.md)
 - [CityCountry](docs/CityCountry.md)
 - [Coordinates](docs/Coordinates.md)
 - [CoordinatesLatitude](docs/CoordinatesLatitude.md)
 - [CoordinatesLongitude](docs/CoordinatesLongitude.md)
 - [GetNetworkRequest](docs/GetNetworkRequest.md)
 - [Line](docs/Line.md)
 - [Network](docs/Network.md)
 - [NetworkBikeSharing](docs/NetworkBikeSharing.md)
 - [NetworkSmartParking](docs/NetworkSmartParking.md)
 - [Station](docs/Station.md)
 - [StationCoordinates](docs/StationCoordinates.md)
 - [StationLinesForStationAdditionalDataInner](docs/StationLinesForStationAdditionalDataInner.md)
 - [VehiclePosition](docs/VehiclePosition.md)


<a id="documentation-for-authorization"></a>
## Documentation For Authorization


Authentication schemes defined for the API:
<a id="ApiKeyAuth"></a>
### ApiKeyAuth

- **Type**: API key
- **API key parameter name**: X-Api-Authentication
- **Location**: HTTP header


## Author




