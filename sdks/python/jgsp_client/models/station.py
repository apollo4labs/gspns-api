# coding: utf-8

"""
    GSPNS / NSmart Public Transport API (Novi Sad)

    Unofficial documentation of the reverse-engineered API behind the GSPNS (JGSP \"Novi Sad\") public transport network — the same API used by the **NSmart** mobile app (`buslogic.nsmartapp`).  The API is served by **BusLogic/NSmart** (`online.nsmart.rs`) and provides:  * the static transport network (cities → stations → line pairs), * real-time vehicle positions and arrival predictions per station, * electronic ticketing / wallet / card management endpoints (login, e-wallet, top-up…).  ## Reverse engineering provenance  * Endpoint paths, class names and the shared API key were recovered from the   NSmart 4.6 Android APK (see [FmasterofU/NSmart-RE](https://github.com/FmasterofU/NSmart-RE)). * Response schemas below were verified against the live API in September 2026. * The API key (`4670f468049bbee2260`) is compiled into the public APK as   `company_api_key` and is the same for every install — treat it as public knowledge.  ## Authentication  Every request must send the header:  ``` X-Api-Authentication: 4670f468049bbee2260 ```  All endpoints in this document include that header in their `security` requirements.  ## Notes & quirks (learned the hard way)  * The giant network payload (`networkextended.php`) is **not valid JSON when served   over a slow connection** — the server truncates / corrupts the stream mid-body   (raw control characters, null-byte runs, dropped chunks). The NSmart app   apparently tolerates this; re-request until you get a parseable body. * `networkextended.php` ignores semantically different `action` values   (`get_network`, `get_trips`, `get_schedule` return the same cities payload as   `get_cities_extended`). * `announcement.php` returns `[{\"success\":false,\"code\":3}]` for an unknown   `station_uid`. For a *known* station with nothing due it returns a single   placeholder entry (`{\"just_coordinates\":\"1\", ...}`) instead of an empty array. * A vehicle without a GPS fix is reported at `lat: \"0.00000000\"`, `lng: \"0.00000000\"`   (often with an empty `garageNo`) — treat `0,0` as \"position unknown\". * `networkextended.php` also answers a plain `GET` with no parameters, and the payload   holds far more than `cities`: the flat `stations` and `lines` lists are what most   clients want (see the `Network` schema and `docs/data-notes.md`). * Timetables (scheduled departures) are **not** served by this API — see   `docs/timetables.md` for the official gspns.co.rs pages that carry them. * `ibfm=TM00000` means \"all lines\"; pass a line code (e.g. `7A`) to filter. * Serbian text is `UTF-8`; strings may come back with JS-style unicode escapes. 

    The version of the OpenAPI document: 1.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from jgsp_client.models.station_coordinates import StationCoordinates
from jgsp_client.models.station_lines_for_station_additional_data_inner import StationLinesForStationAdditionalDataInner
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Station(BaseModel):
    """
    A station (stop) from the network payload.
    """ # noqa: E501
    id: StrictInt = Field(description="Station ID — the value used as `station_uid` and in `Line.all_stations`.", json_schema_extra={"examples": [6801]})
    name: StrictStr = Field(json_schema_extra={"examples": ["Agrovojvodina"]})
    station_id: Optional[StrictStr] = Field(default=None, description="Operator's own station code (not the numeric `id`).", json_schema_extra={"examples": ["0125PB"]})
    slugs: Optional[StrictStr] = None
    city_id: StrictInt = Field(json_schema_extra={"examples": [72]})
    city_name: StrictStr = Field(json_schema_extra={"examples": ["Novi Sad"]})
    coordinates: StationCoordinates
    pairs: Optional[List[StrictInt]] = Field(default=None, description="Line-pair IDs serving the station (may contain repeats).")
    importance_order: Optional[StrictInt] = None
    train_station: Optional[StrictBool] = None
    lines_for_station: List[StrictStr] = Field(description="Display numbers of the lines that stop here.", json_schema_extra={"examples": [["35", "35ČL", "35L"]]})
    lines_for_station_additional_data: Optional[List[StationLinesForStationAdditionalDataInner]] = None
    __properties: ClassVar[List[str]] = ["id", "name", "station_id", "slugs", "city_id", "city_name", "coordinates", "pairs", "importance_order", "train_station", "lines_for_station", "lines_for_station_additional_data"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Station from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of coordinates
        if self.coordinates:
            _dict['coordinates'] = self.coordinates.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in lines_for_station_additional_data (list)
        _items = []
        if self.lines_for_station_additional_data:
            for _item_lines_for_station_additional_data in self.lines_for_station_additional_data:
                _items.append(_item_lines_for_station_additional_data.to_dict() if _item_lines_for_station_additional_data is not None else None)
            _dict['lines_for_station_additional_data'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Station from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "station_id": obj.get("station_id"),
            "slugs": obj.get("slugs"),
            "city_id": obj.get("city_id"),
            "city_name": obj.get("city_name"),
            "coordinates": StationCoordinates.from_dict(obj["coordinates"]) if obj.get("coordinates") is not None else None,
            "pairs": obj.get("pairs"),
            "importance_order": obj.get("importance_order"),
            "train_station": obj.get("train_station"),
            "lines_for_station": obj.get("lines_for_station"),
            "lines_for_station_additional_data": [StationLinesForStationAdditionalDataInner.from_dict(_item) for _item in obj["lines_for_station_additional_data"]] if obj.get("lines_for_station_additional_data") is not None else None
        })
        return _obj


