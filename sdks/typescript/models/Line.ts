/* tslint:disable */
/* eslint-disable */
/**
 * GSPNS / NSmart Public Transport API (Novi Sad)
 * Unofficial documentation of the reverse-engineered API behind the GSPNS (JGSP \"Novi Sad\") public transport network — the same API used by the **NSmart** mobile app (`buslogic.nsmartapp`).  The API is served by **BusLogic/NSmart** (`online.nsmart.rs`) and provides:  * the static transport network (cities → stations → line pairs), * real-time vehicle positions and arrival predictions per station, * electronic ticketing / wallet / card management endpoints (login, e-wallet, top-up…).  ## Reverse engineering provenance  * Endpoint paths, class names and the shared API key were recovered from the   NSmart 4.6 Android APK (see [FmasterofU/NSmart-RE](https://github.com/FmasterofU/NSmart-RE)). * Response schemas below were verified against the live API in September 2026. * The API key (`4670f468049bbee2260`) is compiled into the public APK as   `company_api_key` and is the same for every install — treat it as public knowledge.  ## Authentication  Every request must send the header:  ``` X-Api-Authentication: 4670f468049bbee2260 ```  All endpoints in this document include that header in their `security` requirements.  ## Notes & quirks (learned the hard way)  * The giant network payload (`networkextended.php`) is **not valid JSON when served   over a slow connection** — the server truncates / corrupts the stream mid-body   (raw control characters, null-byte runs, dropped chunks). The NSmart app   apparently tolerates this; re-request until you get a parseable body. * `networkextended.php` ignores semantically different `action` values   (`get_network`, `get_trips`, `get_schedule` return the same cities payload as   `get_cities_extended`). * `announcement.php` returns `[{\"success\":false,\"code\":3}]` for an unknown   `station_uid`. For a *known* station with nothing due it returns a single   placeholder entry (`{\"just_coordinates\":\"1\", ...}`) instead of an empty array. * A vehicle without a GPS fix is reported at `lat: \"0.00000000\"`, `lng: \"0.00000000\"`   (often with an empty `garageNo`) — treat `0,0` as \"position unknown\". * `networkextended.php` also answers a plain `GET` with no parameters, and the payload   holds far more than `cities`: the flat `stations` and `lines` lists are what most   clients want (see the `Network` schema and `docs/data-notes.md`). * Timetables (scheduled departures) are **not** served by this API — see   `docs/timetables.md` for the official gspns.co.rs pages that carry them. * `ibfm=TM00000` means \"all lines\"; pass a line code (e.g. `7A`) to filter. * Serbian text is `UTF-8`; strings may come back with JS-style unicode escapes. 
 *
 * The version of the OpenAPI document: 1.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * A line (one direction variant) from the network payload.
 * 
 * * `line_number_for_display` is the number riders know ("1", "7A") and is unique per
 *   entry. `line_number` / `actual_line_number` are the operator's internal codes and can
 *   differ from it (internal `10` is displayed as `1`, internal `41` as `10`, `101` as
 *   `10APT`) — use the display number when talking to riders, and match on it against
 *   `announcement.php`'s `line_number`.
 * * `all_stations` is every leg of the line **concatenated** (outbound, branch
 *   variants and the return trip) — it is not a single one-way path. Return legs mostly
 *   use their own stop IDs, so the list does not repeat IDs at the turnaround.
 * * A few stop IDs in `all_stations` are not present in `Network.stations`; skip them.
 * * `line_type_color_active` was `#000000` for every line when checked, so it carries
 *   no usable line color.
 * 
 * @export
 * @interface Line
 */
export interface Line {
    /**
     * 
     */
    id: string;
    /**
     * Internal line code.
     */
    lineNumber: string;
    /**
     * Rider-facing line number.
     */
    lineNumberForDisplay: string;
    /**
     * 
     */
    actualLineNumber?: string;
    /**
     * 
     */
    lineTitle: string;
    /**
     * 
     */
    lineTitleForDisplay?: string;
    /**
     * 
     */
    directionIdForDisplay?: LineDirectionIdForDisplayEnum;
    /**
     * 
     */
    priceVariationId?: string;
    /**
     * Line class (`1`, `2` or `3`).
     */
    lineType?: string;
    /**
     * 
     */
    lineTypeColorActive?: string;
    /**
     * 
     */
    lineTypeColorInactive?: string;
    /**
     * Station IDs (as strings) in travel order, all legs concatenated.
     */
    allStations: Array<string>;
}


/**
 * @export
 */
export const LineDirectionIdForDisplayEnum = {
    A: 'A',
    B: 'B',
} as const;
export type LineDirectionIdForDisplayEnum = typeof LineDirectionIdForDisplayEnum[keyof typeof LineDirectionIdForDisplayEnum];


/**
 * Check if a given object implements the Line interface.
 */
export function instanceOfLine(value: object): value is Line {
    if (!('id' in value) || value['id'] === undefined) return false;
    if ((!('lineNumber' in (value as Record<string, any>)) && !('line_number' in (value as Record<string, any>))) || ((value as Record<string, any>)['lineNumber'] === undefined && (value as Record<string, any>)['line_number'] === undefined)) return false;
    if ((!('lineNumberForDisplay' in (value as Record<string, any>)) && !('line_number_for_display' in (value as Record<string, any>))) || ((value as Record<string, any>)['lineNumberForDisplay'] === undefined && (value as Record<string, any>)['line_number_for_display'] === undefined)) return false;
    if ((!('lineTitle' in (value as Record<string, any>)) && !('line_title' in (value as Record<string, any>))) || ((value as Record<string, any>)['lineTitle'] === undefined && (value as Record<string, any>)['line_title'] === undefined)) return false;
    if ((!('allStations' in (value as Record<string, any>)) && !('all_stations' in (value as Record<string, any>))) || ((value as Record<string, any>)['allStations'] === undefined && (value as Record<string, any>)['all_stations'] === undefined)) return false;
    return true;
}

export function LineFromJSON(json: any): Line {
    return LineFromJSONTyped(json, false);
}

export function LineFromJSONTyped(json: any, ignoreDiscriminator: boolean): Line {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'lineNumber': json['line_number'],
        'lineNumberForDisplay': json['line_number_for_display'],
        'actualLineNumber': json['actual_line_number'] == null ? undefined : json['actual_line_number'],
        'lineTitle': json['line_title'],
        'lineTitleForDisplay': json['line_title_for_display'] == null ? undefined : json['line_title_for_display'],
        'directionIdForDisplay': json['direction_id_for_display'] == null ? undefined : json['direction_id_for_display'],
        'priceVariationId': json['price_variation_id'] == null ? undefined : json['price_variation_id'],
        'lineType': json['line_type'] == null ? undefined : json['line_type'],
        'lineTypeColorActive': json['line_type_color_active'] == null ? undefined : json['line_type_color_active'],
        'lineTypeColorInactive': json['line_type_color_inactive'] == null ? undefined : json['line_type_color_inactive'],
        'allStations': json['all_stations'],
    };
}

export function LineToJSON(json: any): Line {
    return LineToJSONTyped(json, false);
}

export function LineToJSONTyped(value?: Line | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'line_number': value['lineNumber'],
        'line_number_for_display': value['lineNumberForDisplay'],
        'actual_line_number': value['actualLineNumber'],
        'line_title': value['lineTitle'],
        'line_title_for_display': value['lineTitleForDisplay'],
        'direction_id_for_display': value['directionIdForDisplay'],
        'price_variation_id': value['priceVariationId'],
        'line_type': value['lineType'],
        'line_type_color_active': value['lineTypeColorActive'],
        'line_type_color_inactive': value['lineTypeColorInactive'],
        'all_stations': value['allStations'],
    };
}

