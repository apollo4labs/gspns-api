/* tslint:disable */
/* eslint-disable */
/**
 * GSPNS / NSmart Public Transport API (Novi Sad)
 * Unofficial documentation of the reverse-engineered API behind the GSPNS (JGSP \"Novi Sad\") public transport network — the same API used by the **NSmart** mobile app (`buslogic.nsmartapp`).  The API is served by **BusLogic/NSmart** (`online.nsmart.rs`) and provides:  * the static transport network (cities → stations → line pairs), * real-time vehicle positions and arrival predictions per station, * electronic ticketing / wallet / card management endpoints (login, e-wallet, top-up…).  ## Reverse engineering provenance  * Endpoint paths, class names and the shared API key were recovered from the   NSmart 4.6 Android APK (see [FmasterofU/NSmart-RE](https://github.com/FmasterofU/NSmart-RE)). * Response schemas below were verified against the live API in September 2026. * The API key (`4670f468049bbee2260`) is compiled into the public APK as   `company_api_key` and is the same for every install — treat it as public knowledge.  ## Authentication  Every request must send the header:  ``` X-Api-Authentication: 4670f468049bbee2260 ```  All endpoints in this document include that header in their `security` requirements.  ## Notes & quirks (learned the hard way)  * The giant network payload (`networkextended.php`) is **not valid JSON when served   over a slow connection** — the server truncates / corrupts the stream mid-body   (raw control characters, null-byte runs, dropped chunks). The NSmart app   apparently tolerates this; re-request until you get a parseable body. * `networkextended.php` ignores semantically different `action` values   (`get_network`, `get_trips`, `get_schedule` return the same cities payload as   `get_cities_extended`). * `announcement.php` returns `[{\"success\":false,\"code\":3}]` for an unknown   `station_uid`. For a *known* station with nothing due it returns a single   placeholder entry (`{\"just_coordinates\":\"1\", ...}`) instead of an empty array. * A vehicle without a GPS fix is reported at `lat: \"0.00000000\"`, `lng: \"0.00000000\"`   (often with an empty `garageNo`) — treat `0,0` as \"position unknown\". * `networkextended.php` also answers a plain `GET` with no parameters, and the payload   holds far more than `cities`: the flat `stations` and `lines` lists are what most   clients want (see the `Network` schema and `docs/data-notes.md`). * Timetables (scheduled departures) are **not** served by this API — see   `docs/timetables.md` for the official gspns.co.rs pages that carry them. * `ibfm=TM00000` means \"all lines\"; pass a line code (e.g. `7A`) to filter. * Serbian text is `UTF-8`; strings may come back with JS-style unicode escapes. 
 *
 * The version of the OpenAPI document: 1.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { StationLinesForStationAdditionalDataInner } from './StationLinesForStationAdditionalDataInner';
import {
    StationLinesForStationAdditionalDataInnerFromJSON,
    StationLinesForStationAdditionalDataInnerFromJSONTyped,
    StationLinesForStationAdditionalDataInnerToJSON,
    StationLinesForStationAdditionalDataInnerToJSONTyped,
} from './StationLinesForStationAdditionalDataInner';
import type { StationCoordinates } from './StationCoordinates';
import {
    StationCoordinatesFromJSON,
    StationCoordinatesFromJSONTyped,
    StationCoordinatesToJSON,
    StationCoordinatesToJSONTyped,
} from './StationCoordinates';

/**
 * A station (stop) from the network payload.
 * @export
 * @interface Station
 */
export interface Station {
    /**
     * Station ID — the value used as `station_uid` and in `Line.all_stations`.
     */
    id: number;
    /**
     * 
     */
    name: string;
    /**
     * Operator's own station code (not the numeric `id`).
     */
    stationId?: string;
    /**
     * 
     */
    slugs?: string;
    /**
     * 
     */
    cityId: number;
    /**
     * 
     */
    cityName: string;
    /**
     * 
     */
    coordinates: StationCoordinates;
    /**
     * Line-pair IDs serving the station (may contain repeats).
     */
    pairs?: Array<number>;
    /**
     * 
     */
    importanceOrder?: number;
    /**
     * 
     */
    trainStation?: boolean;
    /**
     * Display numbers of the lines that stop here.
     */
    linesForStation: Array<string>;
    /**
     * 
     */
    linesForStationAdditionalData?: Array<StationLinesForStationAdditionalDataInner>;
}

/**
 * Check if a given object implements the Station interface.
 */
export function instanceOfStation(value: object): value is Station {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if ((!('cityId' in (value as Record<string, any>)) && !('city_id' in (value as Record<string, any>))) || ((value as Record<string, any>)['cityId'] === undefined && (value as Record<string, any>)['city_id'] === undefined)) return false;
    if ((!('cityName' in (value as Record<string, any>)) && !('city_name' in (value as Record<string, any>))) || ((value as Record<string, any>)['cityName'] === undefined && (value as Record<string, any>)['city_name'] === undefined)) return false;
    if (!('coordinates' in value) || value['coordinates'] === undefined) return false;
    if ((!('linesForStation' in (value as Record<string, any>)) && !('lines_for_station' in (value as Record<string, any>))) || ((value as Record<string, any>)['linesForStation'] === undefined && (value as Record<string, any>)['lines_for_station'] === undefined)) return false;
    return true;
}

export function StationFromJSON(json: any): Station {
    return StationFromJSONTyped(json, false);
}

export function StationFromJSONTyped(json: any, ignoreDiscriminator: boolean): Station {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'name': json['name'],
        'stationId': json['station_id'] == null ? undefined : json['station_id'],
        'slugs': json['slugs'] == null ? undefined : json['slugs'],
        'cityId': json['city_id'],
        'cityName': json['city_name'],
        'coordinates': StationCoordinatesFromJSON(json['coordinates']),
        'pairs': json['pairs'] == null ? undefined : json['pairs'],
        'importanceOrder': json['importance_order'] == null ? undefined : json['importance_order'],
        'trainStation': json['train_station'] == null ? undefined : json['train_station'],
        'linesForStation': json['lines_for_station'],
        'linesForStationAdditionalData': json['lines_for_station_additional_data'] == null ? undefined : ((json['lines_for_station_additional_data'] as Array<any>).map(StationLinesForStationAdditionalDataInnerFromJSON)),
    };
}

export function StationToJSON(json: any): Station {
    return StationToJSONTyped(json, false);
}

export function StationToJSONTyped(value?: Station | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'name': value['name'],
        'station_id': value['stationId'],
        'slugs': value['slugs'],
        'city_id': value['cityId'],
        'city_name': value['cityName'],
        'coordinates': StationCoordinatesToJSON(value['coordinates']),
        'pairs': value['pairs'],
        'importance_order': value['importanceOrder'],
        'train_station': value['trainStation'],
        'lines_for_station': value['linesForStation'],
        'lines_for_station_additional_data': value['linesForStationAdditionalData'] == null ? undefined : ((value['linesForStationAdditionalData'] as Array<any>).map(StationLinesForStationAdditionalDataInnerToJSON)),
    };
}

